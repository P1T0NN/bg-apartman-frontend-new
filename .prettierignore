# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Convex codegen output — `convex dev` rewrites these unformatted on every sync,
# so format-checking them just fights the watcher.
/src/convex/_generated/
/src/convex/auth/component/_generated/

# Notes files carrying pseudo-code snippets (`api.<your>.getCurrentUser`) that Prettier's
# babel parser rejects outright — they document a pattern, they are not runnable code.
FixAuth.md
PRODUCT.md

# Tool-managed, rewritten by Claude Code — not ours to format.
.claude/

# Browser-test harness snapshots — tool-generated, not source.
.playwright-mcp/
